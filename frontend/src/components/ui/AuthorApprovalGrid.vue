<template>
    <v-container>
        <v-snackbar
            v-model="snackbar.status"
            :timeout="snackbar.timeout"
            :color="snackbar.color"
        >
            
            <v-btn style="margin-left: 80px;" text @click="snackbar.status = false">
                Close
            </v-btn>
        </v-snackbar>
        <div class="panel">
            <div class="gs-bundle-of-buttons" style="max-height:10vh;">
                <v-btn @click="addNewRow" @class="contrast-primary-text" small color="primary">
                    <v-icon small style="margin-left: -5px;">mdi-plus</v-icon>등록
                </v-btn>
                <v-btn :disabled="!selectedRow" style="margin-left: 5px;" @click="openEditDialog()" class="contrast-primary-text" small color="primary">
                    <v-icon small>mdi-pencil</v-icon>수정
                </v-btn>
            </div>
            <AuthorApprovalDetails @search="search" style="margin-bottom: 10px; background-color: #ffffff;"></AuthorApprovalDetails>
            <AuthorApprovalList @search="search" style="margin-bottom: 10px; background-color: #ffffff;"></AuthorApprovalList>
            <div class="mb-5 text-lg font-bold"></div>
            <div class="table-responsive">
                <v-table>
                    <thead>
                        <tr>
                        <th>Id</th>
                        <th>Decision</th>
                        <th>DecidedAt</th>
                        <th>DecidedBy</th>
                        <th>Reason</th>
                        <th>작가 ID</th>
                        <th>작가</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr v-for="(val, idx) in value" 
                            @click="changeSelectedRow(val)"
                            :key="val"  
                            :style="val === selectedRow ? 'background-color: rgb(var(--v-theme-primary), 0.2) !important;':''"
                        >
                            <td class="font-semibold">{{ idx + 1 }}</td>
                            <td class="whitespace-nowrap" label="Decision">{{ val.decision }}</td>
                            <td class="whitespace-nowrap" label="DecidedAt">{{ val.decidedAt }}</td>
                            <td class="whitespace-nowrap" label="DecidedBy">{{ val.decidedBy }}</td>
                            <td class="whitespace-nowrap" label="Reason">{{ val.reason }}</td>
                            <td class="whitespace-nowrap" label="작가">
                                <AuthorId :editMode="editMode" v-model="val.authorId"></AuthorId>
                            </td>
                            <v-row class="ma-0 pa-4 align-center">
                                <v-spacer></v-spacer>
                                <Icon style="cursor: pointer;" icon="mi:delete" @click="deleteRow(val)" />
                            </v-row>
                        </tr>
                    </tbody>
                </v-table>
            </div>
        </div>
        <v-col>
            <v-dialog
                v-model="openDialog"
                transition="dialog-bottom-transition"
                width="35%"
            >
                <v-card>
                    <v-toolbar
                        color="primary"
                        class="elevation-0 pa-4"
                        height="50px"
                    >
                        <div style="color:white; font-size:17px; font-weight:700;">AuthorApproval 등록</div>
                        <v-spacer></v-spacer>
                        <v-icon
                            color="white"
                            small
                            @click="closeDialog()"
                        >mdi-close</v-icon>
                    </v-toolbar>
                    <v-card-text>
                        <AuthorApproval :offline="offline"
                            :isNew="!value.idx"
                            :editMode="true"
                            :inList="false"
                            v-model="newValue"
                            @add="append"
                        />
                    </v-card-text>
                </v-card>
            </v-dialog>
            <v-dialog
                v-model="editDialog"
                transition="dialog-bottom-transition"
                width="35%"
            >
                <v-card>
                    <v-toolbar
                        color="primary"
                        class="elevation-0 pa-4"
                        height="50px"
                    >
                        <div style="color:white; font-size:17px; font-weight:700;">AuthorApproval 수정</div>
                        <v-spacer></v-spacer>
                        <v-icon
                            color="white"
                            small
                            @click="closeDialog()"
                        >mdi-close</v-icon>
                    </v-toolbar>
                    <v-card-text>
                        <div>
                            <Number label="ApprovalId" v-model="selectedRow.approvalId" :editMode="true"/>
                            <Date label="DecidedAt" v-model="selectedRow.decidedAt" :editMode="true"/>
                            <Number label="DecidedBy" v-model="selectedRow.decidedBy" :editMode="true"/>
                            <String label="Reason" v-model="selectedRow.reason" :editMode="true"/>
                            <ApprovalDecision offline label="Decision" v-model="selectedRow.decision" :editMode="true"/>
                            <AuthorId offline label="작가 ID" v-model="selectedRow.authorId" :editMode="true"/>
                            <v-divider class="border-opacity-100 my-divider"></v-divider>
                            <v-layout row justify-end>
                                <v-btn
                                    width="64px"
                                    color="primary"
                                    @click="save"
                                >
                                    수정
                                </v-btn>
                            </v-layout>
                        </div>
                    </v-card-text>
                </v-card>
            </v-dialog>
        </v-col>
    </v-container>
</template>

<script>
import { ref } from 'vue';
import { useTheme } from 'vuetify';
import BaseGrid from '../base-ui/BaseGrid.vue'


export default {
    name: 'authorApprovalGrid',
    mixins:[BaseGrid],
    components:{
    },
    data: () => ({
        path: 'authorApprovals',
    }),
    watch: {
    },
    methods:{
    }
}

</script>